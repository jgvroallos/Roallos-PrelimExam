---
 - hosts: all
   become: true
   tasks:

    - name: Set Python3 as Default
      shell: alias python=python3 >> ~/.bashrc
      register: sh_output

    - name: Display Output to CN
      debug:
        msg: "{{ sh_output }}"
